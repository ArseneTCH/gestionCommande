{% extends 'base.html.twig' %}

{% block title %}Burger{% endblock %}

{% block body %}
    <h1>Burger</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ burger.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ burger.nom }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ burger.prix }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_burger_index') }}">back to list</a>

    <a href="{{ path('app_burger_edit', {'id': burger.id}) }}">edit</a>

    {{ include('burger/_delete_form.html.twig') }}
{% endblock %}
