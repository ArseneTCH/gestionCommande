{% extends 'base.html.twig' %}

{% block title %}Complement{% endblock %}

{% block body %}
    <h1>Complement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ complement.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ complement.nom }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ complement.prix }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_complement_index') }}">back to list</a>

    <a href="{{ path('app_complement_edit', {'id': complement.id}) }}">edit</a>

    {{ include('complement/_delete_form.html.twig') }}
{% endblock %}
