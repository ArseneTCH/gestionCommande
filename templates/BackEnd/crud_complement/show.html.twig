{% extends 'base.back.html.twig' %}

{% block title %}Complement{% endblock %}

{% block content %}
    <h1>Complement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ complementSelect.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ complementSelect.nom }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ complementSelect.prix }}</td>
            </tr>
        </tbody>
    </table>
 {% if app.request.attributes.get('_route') == 'complement_edit' %}
                    <h2>Images</h2>
                        {% for image in complementSelect.image %}
                            <div class="row col-sm-3">
                            <a href="{{ path('complement_delete_image', {id: image.id}) }}" data-delete data-token="{{ csrf_token('delete' ~ image.id) }}">
                                <div >
                                    <img src="{{ asset('/uploads/' ~ image.nom) }}" alt="Image" width="150">
                                                                        
                                </div>
                            </a>
                             </div>
                        {% endfor %} 
				{% endif %}  
    <a href="{{ path('complement_management') }}">back to list</a>

    <a href="{{ path('complement_edit', {'id': complementSelect.id}) }}">edit</a>

    {{ include('BackEnd/crud_complement/_delete_form.html.twig') }}
{% endblock %}
