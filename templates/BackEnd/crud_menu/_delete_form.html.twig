<form method="post" action="{{ path('menu_delete', {'id': menuSelect.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ menuSelect.id) }}">
    <button class="btn btn-danger waves-effect">Delete</button>
</form>
